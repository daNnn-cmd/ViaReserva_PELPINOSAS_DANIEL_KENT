@model ViaReservaERP.Models.Enterprise.EnterpriseInquiryViewModel
@{
    ViewData["Title"] = "Enterprise Inquiry";
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Enterprise Inquiry - ViaReserva</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <style>
        :root {
            --vr-navy: #0f2d57;
            --vr-gold: #c5a365;
            --vr-blue: #1e40af;
            --vr-light: #f8fafc;
        }

        body {
            font-family: 'Inter', sans-serif;
            background: url('/img/login-bg-modern.png') no-repeat center center fixed;
            background-size: cover;
            color: #334155;
            min-height: 100vh;
            margin: 0;
            overflow-x: hidden;
        }

        .vr-page-overlay {
            position: fixed;
            inset: 0;
            background: linear-gradient(135deg, rgba(15,45,87,0.88) 0%, rgba(11,18,45,0.96) 100%);
            z-index: 0;
        }

        .vr-nav {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-bottom: 1px solid rgba(15, 45, 87, 0.1);
            padding: 1rem 2rem;
            position: sticky;
            top: 0;
            z-index: 10;
        }

        .vr-nav-brand {
            font-weight: 800;
            font-size: 1.5rem;
            color: var(--vr-navy);
            text-decoration: none;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .vr-nav-links a {
            color: #475569;
            font-weight: 600;
            text-decoration: none;
            margin-left: 2rem;
        }

        .vr-nav-links a:hover {
            color: var(--vr-navy);
        }

        .vr-btn-login {
            background: var(--vr-navy);
            color: white !important;
            padding: 0.6rem 1.5rem;
            border-radius: 8px;
            font-weight: 600;
            text-decoration: none;
        }

        .vr-btn-login:hover {
            background: var(--vr-blue);
        }

        .vr-shell {
            position: relative;
            z-index: 1;
            padding: 48px 16px 72px;
        }

        .vr-headline {
            text-align: center;
            max-width: 820px;
            margin: 0 auto 22px;
        }

        .vr-title {
            font-size: 2.25rem;
            font-weight: 900;
            line-height: 1.15;
            margin-bottom: 10px;
            background: linear-gradient(to right, #fff, var(--vr-gold));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .vr-subtitle {
            color: rgba(255,255,255,0.82);
            line-height: 1.6;
        }

        .vr-card {
            max-width: 980px;
            margin: 0 auto;
            background: rgba(255,255,255,0.96);
            border-radius: 20px;
            border: 1px solid rgba(255,255,255,0.4);
            box-shadow: 0 24px 48px rgba(0,0,0,0.35);
            overflow: hidden;
        }

        .vr-card-body {
            padding: 28px;
        }

        .form-label {
            font-weight: 700;
            color: var(--vr-navy);
            font-size: 0.92rem;
        }

        .form-control {
            border-radius: 12px;
            border: 1px solid rgba(15, 45, 87, 0.15);
            padding: 12px 14px;
        }

        .form-control:focus {
            box-shadow: 0 0 0 4px rgba(197, 163, 101, 0.2);
            border-color: var(--vr-gold);
        }

        .vr-btn-primary {
            background: var(--vr-gold);
            color: var(--vr-navy);
            border: none;
            border-radius: 12px;
            padding: 14px;
            font-weight: 800;
            text-decoration: none;
        }

        .vr-btn-primary:hover {
            background: #d4b57b;
            color: var(--vr-navy);
        }

        .vr-muted {
            color: #64748b;
        }
    </style>
</head>
<body>
    <div class="vr-page-overlay"></div>

    <nav class="vr-nav">
        <div class="container d-flex justify-content-between align-items-center">
            <a href="@Url.Action("Index", "Home")" class="vr-nav-brand">
                <img src="~/img/viareserva-logo.png" alt="ViaReserva" style="height: 32px;" />
                ViaReserva
            </a>
            <div class="vr-nav-links d-none d-md-flex align-items-center">
                <a href="@Url.Action("Index", "Home")#pricing">Pricing</a>
                <a href="@Url.Action("Privacy", "Home")">Privacy</a>
                <a href="@Url.Action("Login", "Account")" class="vr-btn-login">Sign In</a>
            </div>
        </div>
    </nav>

    <div class="vr-shell">
        <div class="vr-headline">
            <div class="vr-title">Premium onboarding inquiry</div>
            <div class="vr-subtitle">For enterprise hotel chains, large resorts, and multi-branch operations. Our team will contact you for custom pricing and setup.</div>
        </div>

        <div class="vr-card">
            <div class="vr-card-body">
                @if (TempData["SuccessMessage"] != null)
                {
                    <div class="alert alert-success">@TempData["SuccessMessage"]</div>
                }

                <form asp-action="SubmitInquiry" asp-controller="Enterprise" method="post">
                    @Html.AntiForgeryToken()

                    <div class="row g-3">
                        <div class="col-md-6">
                            <label asp-for="CompanyName" class="form-label"></label>
                            <input asp-for="CompanyName" class="form-control" />
                            <span asp-validation-for="CompanyName" class="text-danger"></span>
                        </div>

                        <div class="col-md-6">
                            <label asp-for="ContactPerson" class="form-label"></label>
                            <input asp-for="ContactPerson" class="form-control" />
                            <span asp-validation-for="ContactPerson" class="text-danger"></span>
                        </div>

                        <div class="col-md-6">
                            <label asp-for="Email" class="form-label"></label>
                            <input asp-for="Email" class="form-control" />
                            <span asp-validation-for="Email" class="text-danger"></span>
                        </div>

                        <div class="col-md-6">
                            <label asp-for="Phone" class="form-label"></label>
                            <input asp-for="Phone" class="form-control" />
                            <span asp-validation-for="Phone" class="text-danger"></span>
                        </div>

                        <div class="col-md-6">
                            <label asp-for="NumberOfBranches" class="form-label"></label>
                            <input asp-for="NumberOfBranches" class="form-control" type="number" />
                            <span asp-validation-for="NumberOfBranches" class="text-danger"></span>
                        </div>

                        <div class="col-12">
                            <label asp-for="Requirements" class="form-label"></label>
                            <textarea asp-for="Requirements" class="form-control" rows="4"></textarea>
                            <span asp-validation-for="Requirements" class="text-danger"></span>
                        </div>

                        <div class="col-12">
                            <label asp-for="CustomWorkflowNeeds" class="form-label">Custom Workflow Needs</label>
                            <textarea asp-for="CustomWorkflowNeeds" class="form-control" rows="3"></textarea>
                            <span asp-validation-for="CustomWorkflowNeeds" class="text-danger"></span>
                        </div>

                        <div class="col-12">
                            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                        </div>

                        <div class="col-12">
                            <button type="submit" class="vr-btn-primary w-100">Submit Inquiry</button>
                        </div>

                        <div class="col-12 text-center" style="font-size: 0.95rem;">
                            <a href="@Url.Action("Index", "Home")#pricing" style="color: var(--vr-gold); font-weight: 700; text-decoration: none;">Back to pricing</a>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
